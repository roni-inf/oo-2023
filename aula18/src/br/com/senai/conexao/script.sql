/*create table tb_produto(codigo_produto SERIAL primary key,
nome varchar(50), descricao text, preco numeric check (preco > 0) 
not null, quantidade_estoque smallint default 0);
*/
select * from tb_produto;