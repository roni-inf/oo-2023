package exercicios;

import java.util.Scanner;

public class TestePessoa {
	/*    
	    public static void main(String[] args) {
	        
	        Scanner ler = new Scanner(System.in);
	        
	        Pessoa[] pessoas = new Pessoa[4]; 
	        
	        String maior_peso = "zé";
	        String maior_altura = "zé";
	        double mp = 0;
	        double ma = 0;
	        
	        for (int i = 0; i < 4; i++) {
	            pessoas[i]= new Pessoa();
	            System.out.println("Nome:");
	            pessoas[i].nome = ler.next();
	            System.out.println("Peso:");
	            pessoas[i].peso = ler.nextDouble();
	            System.out.println("Altura");
	            pessoas[i].altura = ler.nextDouble();
	    
	            if (pessoas[i].peso > mp) {
	                maior_peso = pessoas[i].nome;
	                mp = pessoas[i].peso;
	            }
	            
	            if (pessoas[i].altura > ma) {
	                maior_altura = pessoas[i].nome;
	                ma = pessoas[i].altura;
	            }
	        }

	        clearConsole();
	        
	        System.out.printf("%s tem o maior peso.\n", maior_peso);
	        System.out.printf("%s tem a maior altura.\n", maior_altura);
	        System.out.printf("A média dos pesos é %.2f \n");
	        System.out.printf("A média das alturas é %.2f");
	    }
	
        public final static void clearConsole()
        {
            try
            {
                final String os = System.getProperty("os.name");

                if (os.contains("Windows"))
                {
                    Runtime.getRuntime().exec("cls");
                }
                else
                {
                    Runtime.getRuntime().exec("clear");
                }
            }
            catch (final Exception e)
            {
                //  Handle any exceptions.
            }
        }

*/	    
	}

